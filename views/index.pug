html
  head
    title= "Hei, NTB!"
    style.
      body {
        background-color: hsl(180, 10%, 90%);
        display: grid;
        font-family: sans-serif;
        height: 100vh;
        place-items: center;
        margin: 0;
        padding: 0;
        
      }

      * {
        border-radius: 3px;
      }

      div {
        max-width: 720px;
        background-color: white;
        padding: 1rem;
        box-shadow: 0 1px 2px rgba(0,0,0,0.07), 
              0 2px 4px rgba(0,0,0,0.07), 
              0 4px 8px rgba(0,0,0,0.07), 
              0 8px 16px rgba(0,0,0,0.07),
              0 16px 32px rgba(0,0,0,0.07), 
              0 32px 64px rgba(0,0,0,0.07);
      }

      h1 {
        text-align: center;
      }

      p {
        padding: 2rem;
        margin: 0;
      }

      p:nth-child(even) {
        background-color: hsl(180, 10%, 95%);
      }
  body
    
    div
      h1="De ti siste tildelingene i Oslo fra Registeret for offentlig støtte"
      each entry in message
          p= entry